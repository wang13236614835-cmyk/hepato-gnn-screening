# 宁显泷 · 工作台（我自己的一份）
> 真实对接已完成 144 次(`results/docking/real/`),新增任务见下表;详见 `reports/真实对接与统一筛选报告.md`。

> 仓库结构：**一级按人分文件夹**（王启龙/宁显泷/衣思淼/代维斯丹/王散曼）；
> 数据、文献、代码、结果等**全队公用一份**（根目录 data/ literature/ src/
> results/ docs/，不复制、以仓库为唯一真相）；每人的任务、打卡、记录
> 在**各自文件夹里再有一份**（本文件＋打卡_宁显泷.html）。
> 公用总表：docs/VERIFY_TASKS.md（全队任务）；docs/MEMBER_WORKBENCH.md（全员导航）。

## 我是谁

**宁显泷** ｜ 算法 · 模型 ｜ 校验线：代码验证（VERIFY_TASKS 附录A·A1）
｜ 名下待核文件 12 个 ｜ 分项时限 **2026-09-13（W1 周五）**

职责一句话：证明"代码跑得对、结果可复现"——一键复现、确定性、45 项独立
数字核验、解析器用例、超参与融合公式核对。

## 当前任务（本周先做）

- **我是谁**：算法/模型负责人，维护真实可运行的 GNN 与 baseline 代码。
- **这周做什么**：先运行默认入口和 `tools/verify_research.py`，确认软件行为、未审核数据阻断和历史模型状态没有越界。
- **为什么现在做**：旧标签不是 MASH assay-level 标签，旧 split 也不能证明泛化；当前 GNN enable gate=closed。
- **怎么做**：默认入口只看状态；需要复现旧教学快照时才使用 `python run_all.py --legacy-demo`，结果写入旧 `results/` 只作历史复核。
- **做完交什么**：软件检查输出、失败项和代码/历史结果分层说明；不追旧 AUC，不产生候选榜。

> 状态来源：[AIDD 主库 qualification_v1](https://github.com/wang13236614835-cmyk/aidd/tree/main/00-%E5%BD%93%E5%89%8D%E7%A0%94%E7%A9%B6/qualification_v1)、本库 `RESEARCH_STATE.md`、`STATUS.json`。

## 历史复核任务（保留，不代表当前科研结论；勾选记录仍有效）

（原任务表保留，旧指标和旧验收值只用于对应版本复现。）

## 我的任务（勾一份，全队表在 docs/VERIFY_TASKS.md §2.1）

| ✓ | 编号 | 要做什么 | 过关口径（出处） |
|---|---|---|---|
| [ ] | N1 | 一键复现 `python run_all.py` | 12 条关键行逐字一致，含 60 候选入库（VERIFY_MANUAL §2.1） |
| [ ] | N2 | 确定性连跑两次 md5 比对 | 输出"一致"（§2.2） |
| [ ] | N3 | 独立数字核验 `python reports/pdf_build/verify.py` | "通过 45 项；不符 0 项"（§2.3） |
| [ ] | N4 | 解析器 5 用例 | 全 OK，重原子数 3/6/20/12/5（§3.1） |
| [ ] | N5 | 超参抽查 gnn.py vs docs/02_model_notes.md | p_drop=0.3、T=30、epoch=300（MANIFEST §3） |
| [ ] | N6 | 融合公式核对 src/scoring/fuse.py | 权重 0.45/0.35/0.20、域外×0.9（文档4.7；衣思淼复核） |
| [ ] | N7 | 对 144 条真实分数复算融合公式(0.45/0.35/0.20),输出 mock vs 真实排名差异表 | Spearman 一致性入 results/rankings/v2/(reports/真实对接与统一筛选报告 §三) |

### 真实版任务(2026-09-03 新增)

- 上表新增任务行(N7)为真实版工作,在 9/20 暑假结果复核完成后开展;真实数据在 `results/docking/real/`(144 次)与 `results/rankings/v2/`,说明见 `reports/真实对接与统一筛选报告.md`。
- 暑假任务行照原计划复核,文件均在原位;学习线与打卡不变。


## 我的文件（12 个，明细=VERIFY_TASKS 附录A·A1）

- 流水线与独立核验（2）：run_all.py、reports/pdf_build/verify.py
- 核心代码（4）：src/chem/smiles_graph.py、src/models/gnn.py、dataset.py、
  baseline.py（代维斯丹复核）
- 评分与文档（2）：src/scoring/fuse.py（衣思淼复核）、docs/02_model_notes.md
- 模型产物（4）：results/metrics/×2、results/predictions/×2


## 我的待核文件（本文件夹，快照副本）

`待核文件/` 里就是我要核验的 **12 个具体文件**（按原目录结构摆放），
逐个打开、对照上一节任务表勾核；来源、大小、md5 见
`待核文件/快照清单.md`。根目录是工作原件（流水线 run_all.py 要用，
不能删）；本副本供我自己逐条阅读勾核，勾完在本文件底部记录表签名。

## 公用资源在哪（一份，别复制）

| 公用内容 | 位置（仓库相对路径） |
|---|---|
| 数据集（raw/processed/splits） | data/ |
| 文献库 | literature/ |
| 代码 | src/、run_all.py |
| 结果（指标/预测/对接/排名/图） | results/ |
| 总纲文档（任务表/核验手册/导航） | docs/ |
| PDF 阅读版 | reports/pdf_all/ |
| 我的学习档案（练习骨架已生成） | learning/宁显泷/（核验：python tools/semester_flow.py --member 宁显泷 check 周号） |

## 怎么操作（五步）

1. **拿仓库**：`git clone https://github.com/wang13236614835-cmyk/hepato-gnn-screening.git`（或 GitHub 页面 Code → Download ZIP）；
2. **先读本文件＋打卡页**：本文件夹 打卡_宁显泷.html（首屏"🧭 我的工作台"）；
3. **动手核验**：按上表逐条执行，预期值以 docs/VERIFY_MANUAL.md 为准；
   `python run_all.py`；`python reports/pdf_build/verify.py`；§3.1 五用例；
4. **记录**：在下表签名；输出存 results/logs/；问题记 docs/VERIFY_TASKS.md §5（A 级先停线报告）；
5. **每周**：打卡_宁显泷.html 按周打卡、周五生成周报发导师。

## 我的完成记录（签名后由 docs/VERIFY_TASKS.md §4 汇总）

| 日期 | 任务编号 | 结果 | 签名 |
|---|---|---|---|
| | | | |
