# 当前推进流

`python tools/semester_flow.py --member 王启龙 dashboard --week 1` 查看本周；plan查看全期；check运行软件检查；record需实际证据路径和结果。旧flow_state.json保留，不导入新验收。

五人HTML由project_plan.json和tools/templates/checkin.html统一生成；`python tools/gen_member_pages.py`。当前snapshot工具只显示工作台路径，不覆盖历史快照。
