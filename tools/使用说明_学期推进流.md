# 当前推进流

`python tools/semester_flow.py --member 王启龙` 查看当前两周卡；`plan` 查看两周总览；`check` 运行软件检查；`record` 需实际证据路径和结果。旧 flow_state.json 保留，不导入新验收；W3–W16 只作历史追溯。

五人HTML由project_plan.json和tools/templates/checkin.html统一生成；`python tools/gen_member_pages.py`。当前snapshot工具只显示工作台路径，不覆盖历史快照。
