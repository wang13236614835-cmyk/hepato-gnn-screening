# GCN关键概念纠正

对称归一化为 D^(-1/2)(A+I)D^(-1/2)，并不保证每行和为1；随机游走归一化D^(-1)(A+I)才有这个性质。用三节点路径含自环演算，端点行和约0.9082，中间行和约1.1498。

前向H1缓存已乘Dropout掩码，W2梯度应直接使用该缓存，再乘一次会重复放大。本次两份GCN对全部参数坐标、两组固定掩码做数值差分；见results/validation/software_checks.json。先理解并复算此反例，再考虑PyG迁移。
